/*#include<stdio.h>
void minMax(int n){
    float f, min=9999, max=-9999;

    for(int i=1; i<=n;i++){
        printf("\nIngrese un numero flotante: ");
        scanf("%f", &f);
        if(f<min)
            min=f;
        if(f>max)
            max=f;
    }
    printf("El min es %.2f, y el max es %.2f", min, max);
}

int main(){
    int n;
    printf("Ingrese la cantidad de numeros flotantes a leer: ");
    scanf("%d", &n);
    minMax(n);
    return 0;
}
*/
